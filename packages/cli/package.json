{
  "name": "modex",
  "version": "0.2.0",
  "description": "Modex CLI — a Codex-style terminal coding agent on the @modex/core engine.",
  "license": "MIT",
  "type": "module",
  "bin": { "modex": "./bin/modex.js" },
  "files": ["bin", "dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": { "@modex/core": "0.2.0" }
}
